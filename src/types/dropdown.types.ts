export interface DropdownMenuItem {
  label: string;
  value: string;
}

export enum ResultSort {
  ASC = 'asc',
  DESC = 'desc',
  HEIGHT = 'height',
  WEIGHT = 'weight',
}

export enum PageSize {
  TEN = '10',
  TWENTY = '20',
  THIRTY = '30',
}
